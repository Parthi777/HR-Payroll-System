<?xml version="1.0" encoding="utf-8"?>
<!--
  DEBUG BUILDS ONLY. This file lives in src/debug, so it is merged into debug
  builds and is absent from anything released — the release APK gets
  src/main/res/xml/network_security_config.xml, which permits no cleartext at
  all.

  It used to live in src/main with a comment asking whoever cut a release to
  remove the domains first. There is no release source set for it to be removed
  from, so every released APK carried these exceptions.

  Cleartext HTTP to a local backend during development. Android blocks cleartext
  by default from API 28; this project's minSdk is 24, so on an older device it
  would otherwise be permitted everywhere.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">192.168.1.47</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
